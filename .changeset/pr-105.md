---
'@jbpark/use-hooks': minor
---

The `useElementScroll` hook now accepts an optional `threshold` option to customize the 'at bottom' detection, and it also observes child elements and their size changes to accurately track scroll positions.
