---
---

Docs-only change: the demos moved from `src/demo` into `website/src/demo`, so
the published package (`dist`, built from `src/hooks` + `src/index.ts`) is
untouched. Empty changeset so no version is released for it.
