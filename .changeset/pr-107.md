---
'@jbpark/use-hooks': minor
---

Add support for measuring the viewport instead of an element/document.body, useful when no container/ref is attached and the breakpoint should reflect the viewport rather than document.body's box.
