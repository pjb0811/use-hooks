---
'@jbpark/use-hooks': minor
---

useViewport now returns a fresh, plain-object snapshot of the viewport on every read, rather than the mutable window.visualViewport instance, to ensure accurate re-renders on changes.
