---
'@jbpark/use-hooks': minor
---

The `useElementPosition` hook now supports string selectors and handles cases where the target element may not exist yet when the hook first runs, or where the DOM node behind a ref gets swapped out.
