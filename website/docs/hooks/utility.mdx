---
sidebar_position: 6
title: Utility
---

import DemoTheme from '@site/src/components/DemoTheme';
import ImageDemo from '@site/src/demo/use-image-demo';
import MergedRefDemo from '@site/src/demo/use-merged-ref-demo';

# Utility

Small standalone utilities that don't fit the other categories.

<DemoTheme>

## useMergedRef {/* #use-merged-ref */}

<MergedRefDemo />

```tsx
const mergedRef = useMergedRef(forwardedRef, internalRef);

<div ref={mergedRef} />;
```

## useImage {/* #use-image */}

<ImageDemo />

```tsx
const { loading, error, loaded, retry, attemptCount } = useImage(src, {
  retryCount: 1,
});
```

</DemoTheme>
